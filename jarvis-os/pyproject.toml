[project]
name = "jarvis-os"
version = "0.1.0"
description = "JARVIS AI Operating System — Sprint-0: Foundation Bootstrap"
requires-python = ">=3.10"
readme = "README.md"

[tool.pytest.ini_options]
testpaths = ["tests"]
