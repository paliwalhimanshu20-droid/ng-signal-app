"""
Warehouse foundation package version.

This is deliberately independent from the *data* schema version (tracked by
VersionManager in metadata/version_manager.py). This number tracks the
Python package/code itself.
"""

__version__ = "1.0.0"
